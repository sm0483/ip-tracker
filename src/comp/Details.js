import { useLocation } from "./Context/LocationContext";

const Details = () => {
    const {result}=useLocation();
    

    return(
      <section>
        
      </section>
    )
}
 
export default Details;